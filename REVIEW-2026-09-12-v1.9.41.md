# Outmap 1.9.41 优化与修复说明

## 问题根因与修复分析

### 1. 途径点飞过去后很淡或不显示、再次点击才显示的根因彻底解决

- **根因分析**：
  1. 3D地形下远距离飞向途径点时，相机带有约 50° 的俯仰角（Pitch）。在飞跃发起时，目标区域的高精度 DEM（数字高程模型）瓦片尚未下载至内存，计算得出的地面高度为 0（海平面水平）。
  2. 飞抵目标点后，MapLibre 的渲染缓存机制 `submitGeoJSONChanges(source, data)` 会比对内存中的 GeoJSON 特征。由于途径点的经纬度和 ID 在内存中并未发生变化，函数判定“数据无变动”直接提前 `return`，**未向底层 GPU 几何源调用 `source.setData()`**。
  3. 与此同时，网络瓦片流式加载完毕，三维山体真实地形拔地而起（数十米至数百米高程）。由于底层 GeoJSON 标记未触发重新投影，途径点的圆形图层被深埋于三维山体网格下方；在 WebGL 深度测试（Depth Test）开启状态下，大部分被山体实体遮挡，仅有微弱的光晕（Halo）或外浮标签露出，造成用户看到的“**飞过去之后很淡或者不显示**”。
  4. 当用户在目标位置再次点击该途径点时，由于此时高精 DEM 已经驻留内存，点击操作触发了状态重绘，标记点瞬间吸附到真实山体表面，造成用户看到的“**然后再点才显示出来**”。
- **修复措施**：
  1. 为 `submitGeoJSONChanges(source, data, force = false)` 引入强刷参数 `force`：当 `force === true` 时主动清除 `geoJSONRenderCache` 缓存，无条件向底层图层源投递最新地形投影数据；
  2. 强化 `syncRouteMarkersVisualState(mapInstance, force = false)` 并使其向下透传 `force` 标记；
  3. 在 `refreshAllRouteMarkersElevation(map)` 中直接调用 `syncRouteMarkersVisualState(m, true)` 和 `renderWaypointMarkersOnMap()`，确保高程沉降后即刻浮出地面；
  4. 在精确飞行函数 `flyToLocationPrecisely` 中增加到达后两段式 DEM 沉降对齐（分别在到达后 260ms 与 680ms 自动触发刷新），等待高精 DEM 瓦片完全解码后自动将途径点吸附至山地表面；
  5. 在途径点标签点击（`renderViaList`）、路线起点与终点点击（`staticStartTag` / `staticEndTag`）、以及地图上路线控制点点击（`bindRoutePointLayerEvents`）时，统一通过 `getRealElevation(map, coords)` 计算真实地形高程并传入 `flyToLocationPrecisely` 的 `elevation` 参数，飞行到达时直接贴合山表。

---

### 2. 全端交互一致性强化（PC桌面版、Web网页版、iPad/平板、手机端）

- **全平台收藏点地图点击唤起操作菜单**：
  - 去除仅移动端唤起菜单的限制，全面升级为桌面端左键、移动端/平板触屏轻触均在飞掠聚焦的同时唤起气泡操作菜单（含重命名、改类型、删除）；
  - 保留桌面右键（`contextmenu`）与移动端/平板 450ms 容差长按（`touchstart`/`touchmove` 10px 抖动容差），多端交互体验统一。
- **路线卡片与分类抽屉小屏视口自适应上翻**：
  - 收藏路线卡片的“更多 (•••)”菜单与右键菜单增加自适应判定：若 `y + height > viewportBottom - 16`，自动向上反转弹出，绝不被视口底边裁切；
  - 收藏分类 Tab 增加 450ms 触控长按手势与“•••”更多操作菜单，同样支持向上反转弹出；
  - 关键操作项（重命名、导出 GPX、删除）统一绑定 `click` 与 `touchend` 双通道监听。
- **全量增删改即时云同步**：
  - 新增地点（`add_waypoint`）、保存路线（`save_route`）、新增分类（`add_folder`）、删除分类（`delete_folder`）等全部配置 `immediate = true` 即时同步，跳过防抖延迟直推 Cloudflare R2，实现手机与桌面毫秒级互通。

---

### 3. 测试与环境防卡死防护

- **Watchdog 机制全面覆盖**：
  - 所有自动化测试脚本（包含 `scripts/test-v1.9.41.cjs` 及后续测试）统一配置 30 秒硬件看门狗（`watchdog` 定时器），发生异常或渲染停滞时主动退出，绝不无限挂起；
  - 严禁调用无超时保护的网络命令（如裸 `curl`），所有云端状态校验均采用 Node.js `https.request` 并设置明确的连接超时与读取超时。

---

## 验证与发布

- 运行 `node --check src/app.js`：**PASS (0 错误)**
- 运行 `node --check scripts/test-v1.9.41.cjs`：**PASS (0 错误)**
- 运行 `npx electron scripts/test-v1.9.41.cjs`：**PASS (100%)**
- 运行 `npm run pretest:release`：**PASS (100%)**
- 运行 `npm run test:release`：**PASS (18个测试套件全绿通过)**
- 自动打包构建 `app.asar` 并推送到 Cloudflare R2 生产节点（`v1.9.41`）。
