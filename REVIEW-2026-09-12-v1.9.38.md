# Outmap 1.9.38 优化与修复说明

## 变更背景与需求

1. **途经点视觉升级**：
   - 将此前 v1.9.33 引入的紫色（`#6366f1`）全面调整为质感纯正、清晰明朗的高阶天蓝色（`#0284c7`），与规划面板途经点序号徽章保持严格统一。
   - 重叠点与密集聚合点（Cluster）维持穿透力强、温暖醒目的琥珀橙色系（`#f59e0b` / `#d97706` / `#b45309`），形成鲜明直观的起点（绿）- 终点（红）- 途经点（蓝）- 密集聚合（橙）四维视觉体系。
2. **桌面端 ESC 键逃逸闭环体验优化**：
   - 地图途经点拖拽态：支持按 `Escape` 立即取消拖拽并原位还原标记，不触发误重算与误提交。
   - 途经点列表排序拖拽态：支持按 `Escape` 立即复位还原原始顺序并退出拖拽态。
   - 输入与下拉候选层级：按 `Escape` 优先收起候选联想框；再次按下让输入框脱离焦点，杜绝一次 ESC 误关整个路线面板。
   - 路线面板：在无输入焦点与子菜单时按 `Escape` 平滑收起路线面板，并协同清理下拉残余。
3. **测试套件规范化**：
   - 修复 `test-layer-route-separation.cjs` 中异步读取 MapLibre 5.x 工作线程 GeoJSON 数据的断言问题。

## 验证

- 运行 `npm run test:desktop-performance`：**PASS**
- 运行 `npm run pretest:release`：**PASS**
- 运行 `npm run test:release`：**PASS (100%)**
- 覆盖高低 DPI 自适应、相机大跨度飞行防抖、多途径点路线连续规划与排序、原生聚合及 GPX/KML/GeoJSON 导入导出全链路。
