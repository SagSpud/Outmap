# Outmap 1.9.39 优化与修复说明

## 变更背景与需求

1. **彻底解决手机浏览器“收藏地点重命名不生效”缺陷**：
   - **移动端键盘无提交表单根因修复**：模态输入弹窗（`showFluentPrompt`）内部包装原生 `<form class="fluent-prompt-form" action="javascript:void(0);">`，输入框配置 `enterkeyhint="done"` 与防拼音输入法截断监听，支持在 iOS Safari / Android Chrome 虚拟软键盘右下角“完成/回车”一键提交。
   - **消除虚拟键盘收起导致丢点击（Lost Click）的 WebKit 底层缺陷**：在确定按钮（`.btn-prompt-confirm`）上监听 `pointerdown` 优先截获提交，杜绝点击时由于输入框失焦软键盘回弹、视口位移导致 `touchend` 判定漂移而吞掉 `click` 事件的问题。
   - **移动端软键盘遮挡与误触防护**：输入弹窗在移动端（`max-width: 768px`）采用 `height: 100dvh` 与靠顶弹性对齐（`align-items: flex-start; padding-top: max(24px, calc(16dvh + env(safe-area-inset-top)))`），使弹窗完全置于软键盘上方安全交互区域；引入遮罩双向点击判定，防止键盘收起时误触遮罩退出。

2. **跨设备云端漫游 LWW (Last-Write-Wins) 冲突仲裁机制升级（根治同步冲刷死循环）**：
   - **时间戳仲裁**：为收藏点（Waypoints）、路线（Routes）和分类（Folders）全面引入 `updatedAt` 时间戳属性，在地点新增、重命名、类型变更、路线保存与重命名、分类重命名时均刷新 `updatedAt: Date.now()`。
   - **双向合并根因修复**：重构 `mergeWaypoints`、`mergeRoutes`、`mergeFolders`。当同一 ID 或空间特征的点位/路线/分类发生冲突时，通过对比 `updatedAt` 实施精准的“最后修改者胜出”原则，彻底消除了“手机改名同步到云端后，被电脑端旧名称本地缓存冲刷覆写并再次倒灌回云端”的死循环。
   - **移动端切后台即时同步**：监听 `pagehide` 与 `visibilitychange` 事件，在用户切换手机浏览器 App 或关闭标签页时立即触发未执行的云同步防抖队列，确保漫游数据零丢失。

3. **移动端地点与路线管理交互体验强化**：
   - **显式操作入口**：在收藏地点列表卡片（`.fav-item-card`）与路线列表卡片（`.fav-route-card`）右侧新增直观的 `•••` 更多操作按钮（`.fav-item-more-btn`），手机端点击即可精准弹出管理菜单（重命名、修改类型/分类、导出、删除），与 500ms 长按手势双轨并存。
   - **手势防干扰**：为列表卡片补充 `-webkit-touch-callout: none; user-select: none;`，彻底禁用移动端长按触发的系统放大镜与文本复制气泡干扰。

4. **架构与兼容性保障**：
   - 保持零轮询、无空转、纯净事件驱动架构（`idleRenders: 1`）；
   - 新增 `test-v1.9.39.cjs` 自动化单元与集成测试，并纳入 `npm run test:release` 自动化回归全量测试套件。

## 验证

- 运行 `npm run test:desktop-performance`：**PASS**
- 运行 `npm run pretest:release`：**PASS**
- 运行 `npx electron scripts/test-v1.9.39.cjs`：**PASS (100%)**
- 运行 `npm run test:release`：**PASS (100%)**
- 自动构建 `app.asar` 并推送到 Cloudflare R2 生产节点。
