# Outmap v1.9.35 桌面地图运动性能优化

## 优化内容

- 地图拖动、缩放和飞掠结束后，下载线程延迟 650ms 再恢复满并发，避免与落地点瓦片上传、DEM 解码和文字排布同时争用 CPU、GPU 与磁盘。
- 新增桌面 MapLibre 运动调度器。100% DPI 保持原生渲染；150% 与 200% DPI 只有在持续运动且检测到真实慢帧时才临时调整运动分辨率，停止后恢复当前显示器原生 DPR。
- 长距离飞掠期间只暂退高密度 POI、门牌与建筑名称。3D 地形、3D 建筑、道路、路线、收藏点和途径点始终保留；落地后使用原生图层过渡恢复。
- DEM 分批到达和相机落地触发的路线高程刷新进行合并，并等待 MapLibre 稳定后统一计算；设置有最长等待兜底，不会因缺失瓦片永久等待。
- 高程夸张滑块的地形更新通过 requestAnimationFrame 合并，同一帧只向 MapLibre 提交最后一个值。

## 稳定性保证

- 自适应策略不运行永久 RAF，静止地图没有新增重绘循环。
- 快速替换飞掠、用户中断和连续长途飞掠都会完整恢复图层可见性、透明度、过渡状态及原生像素比。
- 桌面专用的动态分辨率与长途分层减载不会应用到手机和普通网页环境。
- 不删除、不迁移、不扫描或改写现有离线地图、收藏地点和收藏路线。

## 验证

- 100%、150%、200% DPI 自适应和原生清晰度恢复测试通过。
- 2D、50°、70° 3D、跨区域飞掠、快速替换和用户中断测试通过。
- 路线规划、多途径点、收藏聚合、离线清单、下载背压、轨迹导入和静止 GPU 发布级回归全部通过。
