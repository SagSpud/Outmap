# Outmap v2.0.12 彻底解决 3D 高山及全层级缩放回弹跳动与位移

## 视频回放深度分析与根因

用户反馈并提供了两段关键录屏：
1. **`1.mp4`（3D 高山贴近大跳动）**：
   - 用户在 3D 地形模式下，以 ~53° 俯仰角对着高山连续滚动滚轮放大。
   - 当放大到 16.5 级时（00:11），画面瞬间暴力回弹至 14.1 级（突降 2.4 级！）；随后在 00:19 放大至 16.4 级时，再次瞬间回退至 13.3 级（突降 3.1 级！）。
2. **`屏幕录制 2026-09-14 224100.mp4`（全层级缩放抽动与位移）**：
   - 用户在各层级（包括 L8~L12）双向滚轮变倍缩放、拖拽与平移，偶发出现卡顿与轻微回弹位移。

### 根因挖掘（MapLibre 6.9 原生地形相机避障 vs 用户交互相机守护冲突）

- MapLibre GL JS 在开启 3D 地形 (`map.setTerrain`) 时，内部具有原生内置的碰撞避障与防穿模机制 `_elevateCameraIfInsideTerrain`。
- 当相机视角贴近山体时，如果继续放大或大倾角会导致相机进入地表以下，MapLibre 的原生渲染循环会自动计算出安全的最大缩放级别与仰角（例如将 zoom 适度降到 14.1，保证相机视点不被埋入山体）。
- 然而，此前为了防止滚轮惯性回退而引入的 `applyZoomDirectionGuard`（防回弹守护）全局监听了每一次相机 transform。当 MapLibre 避障机制把 zoom 动态微调或钳制时，守护机制将其误判为“违背用户意图的反向回弹”，在每一帧强行将 `transform.zoom` 覆盖回 `currentZoom`（16.5 级）。
- **结果**：MapLibre 原生避障逻辑与 JS 守护逻辑在 60FPS 下发生持续冲突拉锯。当用户停下滚轮超过 1200ms 超时后，守护机制解除，MapLibre 原生避障瞬间生效，将积累的避障差值一次性生效，造成从 16.5 暴跌回 14.1 / 13.3 的剧烈画面跳跃！
- 此外，MapLibre 在非中心点缩放时，是中心点经纬度（center）与 zoom 严格联动计算的。任何在 JS 层单方面拦截/修改 `transform.zoom` 的做法，都会破坏光标像素锚点的一致性，引发缩放后画面的轻微偏移与跳动。

## 修复方案

1. **彻底移除用户交互层面的所有相机干预与守护机制**：
   - 彻底删除 `zoomGuards`、`applyZoomDirectionGuard`、以及在 canvas/container 上的 `wheel`、`dblclick`、`keydown`、`touchstart`、`touchmove`、`touchend` 全局事件拦截。
   - `OutmapLocationCamera.install(map)` 净化为幂等的无副作用重置（`map.setTransformCameraUpdate(null)`），不再对用户的鼠标滚轮、双指缩放施加任何人为约束。
2. **完全交还 MapLibre 6.9 原生 GPU 与物理碰撞避障引擎**：
   - 用户的滚轮缩放、双指捏合缩放、倾斜平移完全由 MapLibre 原生处理，光标锚点 100% 像素精确，0 偏移，0 假死。
   - 面对 3D 高山，MapLibre 原生地形平滑过渡避障正常工作，不再有拉扯和突发暴跌跳跃。
3. **精准保留飞掠平滑高程渐变，且落地即完全销毁**：
   - 在程序触发的受管飞行（`fly()`）期间，平滑渐变高程以消除地形加载跳跃。
   - 一旦飞行动画完成（`moveend`）或被用户手势中断，立即调用 `dispose()`，将 `setTransformCameraUpdate(null)` 清空，绝不在用户后续操作中留下任何残留。

## 自动化测试与验证

- `scripts/test-camera.js`（桌面端 9 项严苛场景）：
  - `nearby`：误差 `7.88e-8 px`
  - `Lhasa app`：误差 `5.17e-9 px`
  - `Lhasa close`：误差 `2.88e-9 px`
  - `2D`：误差 `1.93e-9 px`
  - `overview`：误差 `8.23e-12 px`
  - `steep`（70° 极限俯仰）：误差 `6.95e-9 px`
  - `rapid`、`instant`、`late DEM`：全部超高精度通过，`correctiveJumps: 0`。
- `scripts/test-camera.js --mobile`（移动端 390×844，含 Drawer 避让抽屉）：全部 11 项用例亚像素级通过。
- `npm run pretest:release`：10 项发布前回归测试全部一次性通过。
- `npm run test:release`：包括 GPX/KML/GeoJSON 导入导出、路线重排、3D 瓦片连续性、离线瓦片兼容性等全部通过。

## 发布部署

- 版本号升至 `2.0.12`。
- 构建打包并部署至 Cloudflare R2，线上校验通过。
- 提交并推送到 GitHub `main` 分支。
