# 收藏夹交互与近期版本复核

基线：67f0c2a（1.9.23）。本次修改纳入 1.9.24，桌面热更新已上传。

## 已修改

- 收藏分类使用 Pointer Events 拖动和 Web Animations 平滑让位；鼠标拖动、触屏横向拖动、边缘滚动、Esc/取消恢复、释放后保存排序。每次重新绑定均清理上一组事件，空闲不保留动画循环。DOM 控件排序属于浏览器交互，不适合交给 MapLibre。
- 收藏地图符号用固定 Path2D 图形注册为 MapLibre sprite；收藏列表和类型菜单使用同源 SVG。避免不同手机 Emoji 字体导致空白或图形差异。
- 收藏飞掠原始海拔传递给相机，高程夸张仅计算一次。修复地图容器有左偏移时，相机可视工作区坐标混用。
- 单个 GeoJSON Point/Feature 也支持点位导入，收藏导入拒绝空坐标与越界坐标。
- 中止下载不再提前释放运行锁，避免旧任务结束覆盖新任务状态。
- HTTP 404/410 不计为已下载完成，避免任务结束把实际缺片提升为绿色完成。
- 修复下载背景样式错误嵌套在移动端媒体条件内，确保桌面下载时也生效。

## 验证

- 收藏拖动专项：顺序实时变化、仅提交一次、Esc 恢复、浮动预览清理、8 类固定图形生成。
- 相机测试：2D、50/70 度、快速替换与取消、延迟地形、移动面板。
- GPX/KML/GeoJSON/原生路线导入回归。
- 原生地图图层及空闲渲染回归：旧 FPS 标签断言随 1.9.22 的实际移除更新为保留真实空闲帧数检查；毛玻璃统一检查适配 10/20px。

## 尚未证实

白鹭金岸原始文件尚未定位，无法确认其坐标是否为 WGS84、GCJ-02 或本身记录错误。已修复的双重海拔换算可能影响 3D 飞掠，但不能据此认定该地点的地理偏差已解决。没有对用户既有坐标批量转换。

历史回复“404 不会重复下载”不准确：当前没有持久化 404 清单。也不能仅由截图认定 404 属于矢量层或无人区。后续若要缓存此类响应，应按源版本及有效期记录，避免永久掩盖源站暂时缺片。

原代码按扩展名接受任意零字节 PBF，无法区分旧损坏文件与确认过的合法空瓦片；这是需独立迁移设计的问题，本次未扫描或修改用户离线目录。
