# Outmap v2.0.18 稳定性修复与发布记录

## 本版修复

- 3D 飞掠中只接受一次目标高程和锚点高程，避免 DEM 分级加载时反复拉动相机。
- 使用 MapLibre 公开相机变换接口保持 3D 落点在屏幕中间偏下，飞掠结束后不执行第二次动画。
- 为 50°/70° 复杂地形滚轮缩放增加方向和单次幅度保护，防止地形碰撞校正导致反向回缩。
- 无 ID 的历史收藏路线改用确定性身份，无时间戳数据不再每次读取时冒充新数据。
- 收紧旧路线兼容合并规则，同名但起终点不同的路线不再误合并、误改名或误删除。
- 搜索 LRU 缓存增加 10 分钟过期，收藏点新增、改名、移动、分类或删除后立即失效。
- 离线瓦片 ETag 改为真实内容指纹，新下载文件提交后清理对应的内存旧缓存。

## 验证

- `npm run pretest:release`：通过。
- `npm --ignore-scripts run test:release`：通过。
- 真实在线 DEM 桌面与移动端飞掠：通过，关键落点误差低于 1 px。
- 2D/50°/70°、L3.9–L16.8 及不同鼠标锚点的 114 组滚轮缩放用例：通过。
- MapLibre 6.9 旧 OSM/DEM 离线格式兼容：通过，无需重新下载。
- 收藏路线身份、改名、删除墓碑和并发云同步：通过。

## 发布产物

- `dist/Outmap-Setup-2.0.18-x64.exe`
- `dist/Outmap-2.0.18-win.zip`
- `dist/win-unpacked/Outmap.exe`
- R2 增量更新：`Outmap/app.asar` 与 `Outmap/version.json`

现有 OSM、DEM、等高线及离线地图目录均未改动。
