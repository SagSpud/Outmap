# Outmap v2.0.9 单轨飞掠修复

## 录屏结论

- 新录屏最后一次远距离飞掠约在 7.2 秒开始，8.1～8.3 秒出现一组独立于主飞行的缩放、旋转和位移，正是肉眼看到的“到达后又拉一下”。
- 根因是 2.0.8 在主 `moveend` 后仍会启动 180ms 地形高程收尾，必要时再启动 140ms 重锚定。它解决了目的地 DEM 晚到时的空白/穿地形，但相机轨迹实际上仍有两到三段。
- 真实在线高山 DEM 测试确认：如果目的地海拔未在主飞行中进入相机状态，MapLibre 会暂时沿用出发地高程，落点可偏离 844～2109px；事后校正必然可见。

## 修复

- 单次定位只保留一次原生 `flyTo`/`easeTo`；`moveend` 之后不再调用任何相机移动方法。
- 收藏点、收藏搜索结果、路线起终点/途经点以及导入路线中的已有海拔，在主飞行内按地形夸张系数连续插值，不再等落地后补偿。
- 没有已有海拔时，目的地 DEM 若在飞行途中可采样，会从当前帧开始在剩余主动画内连续吸收，不产生第二条动画轨迹。
- DEM 直到落地后才到时只触发地图重绘，不改变 center、zoom、pitch 或 bearing；同时移除可能形成持续重绘的 `idle` 链路。
- GPX/KML/GeoJSON 导入路线保留坐标第三维海拔，避免导入点首次定位丢失高度提示。

## 验证

- 真实在线高山 DEM：50°、70°、L13、L14.8、L15，落点误差降至 0.07～0.29px。
- 本地确定性 6000m DEM：桌面、手机、2D/3D、远近距离、连续飞掠全部通过。
- 明确断言一次定位只有一个原生飞行，DEM 晚到后相机 zoom/pitch 不再变化。
- 完整发布测试通过：MapLibre 6.9、离线 DEM/OSM、114 组滚轮矩阵、68 组 3D 分数层级、路线 62 点、收藏/图层、静止 GPU、高分屏和移动端。

## 数据兼容

- 未修改离线 DEM、OSM、等高线、收藏或路线的存储格式和目录；现有离线地图无需重新下载。
