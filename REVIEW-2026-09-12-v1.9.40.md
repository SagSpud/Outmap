# Outmap 1.9.40 优化与修复说明

## 后台历史记录排查与根因分析

通过 Cloudflare R2 存储桶后台直查（`Outmap/sync/user_cuihm.json`），检索到用户最新同步时间戳为 `2026-09-12T11:39:37.330Z`。
分析其同步载荷内容：
- 用户当前唯一收藏点：`{ id: 'wp_1789198399403', name: '白鹭金岸', type: 'view', updatedAt: 1789142400000 }`；
- 同步版本标记为 `1.9.39`，说明用户确实在手机浏览器加载了 `1.9.39`；
- 但由于旧版在手机端存在的交互与缓存问题，导致重命名操作并未在手机浏览器成功触发或提交：

1. **Cloudflare 强缓存锁定导致手机浏览器未执行新脚本**：
   - `curl -I https://map.053999.xyz/app.js?v=1.9.39` 显示 `cache-control: public, max-age=31536000, immutable`，Cloudflare 边缘节点命中 `cf-cache-status: HIT`，手机 Safari/Chrome 本地强缓存了旧版 `app.js?v=1.9.39`，即使刷新网页也读取磁盘缓存。
   - **修复**：全量版本升级至 `1.9.40`，更新 `index.html` 内所有静态资源引用查询参数（`app.js?v=1.9.40`），强制所有移动端和边缘节点破除强缓存。

2. **地图上点击收藏点图标在手机端无法弹出操作菜单**：
   - 之前在地图上轻触标记点仅触发 `flyTo` 视角移动；移动端没有鼠标右键（`contextmenu`），而 `touchstart` 长按在手指有微小抖动时因无容差被即刻取消，导致手机用户在地图上根本无法调出重命名菜单。
   - **修复**：
     - 在手机/触屏环境下，点击地图收藏点图标除飞行外，直接自动在触控位置唤起操作菜单（包含重命名、改类型、删除等）；
     - 长按判定增加 `10px` 触控微动容差（`Math.hypot <= 10`），杜绝手指轻微颤抖被误判为滑动而取消长按。

3. **操作气泡菜单小屏视口自适应与触控绑定缺失**：
   - 旧版气泡菜单在屏幕下半部弹出时因未做视口底边高度校验，可能部分伸出屏幕外，且移动端点击菜单项时只有 `click` 监听无 `touchend`。
   - **修复**：
     - 气泡菜单增加视口高度检测，当 `y + height > viewportBottom - 16` 时自动向上反转弹出，确保“重命名”和“删除”按钮完整可见；
     - 将菜单宽度由原本拥挤的 `94px` 放大为 `128px`，触控热区由 `32px` 扩大为 `38px`；
     - 菜单项绑定 `click` 与 `touchend` 双通道监听并阻止冒泡与默认行为。

4. **输入弹窗 DOM 结构与输入法（IME）提交问题根治**：
   - Safari 在带有 `display: contents;` 的表单容器中存在表单提交行为偶发中断的问题；
   - 移动端拼音输入法尚未按“确认”而直接点击确定按钮时，输入框内未提交的合成字符可能被丢弃。
   - **修复**：
     - 将 `.fluent-prompt-card` 本身作为原生 `<form class="fluent-prompt-card fluent-prompt-form" action="javascript:void(0);">`，移除不稳定的 `display: contents;`；
     - 在提交（`submitPrompt`）的第一步先执行 `input.blur()`，强制手机系统输入法将拼音输入缓冲区刷入 `input.value`；
     - 确定按钮同时支持 `pointerdown`、`touchend`、`click` 及表单原生 `submit`（键盘“完成”键）。

5. **重命名后即时触发云同步**：
   - 地点重命名、分类重命名、路线重命名后，直接以 `immediate = true` 参数调用 `triggerRealtimeCloudSync(..., true)`，跳过防抖延迟即刻推送 Cloudflare R2，并弹出轻量 Toast 提示用户“已重命名为...”。

---

## 验证与发布

- 运行 `npm run test:desktop-performance`：**PASS**
- 运行 `npm run pretest:release`：**PASS**
- 运行 `npx electron scripts/test-v1.9.40.cjs`：**PASS (100%)**
- 运行 `npm run test:release`：**PASS (100%)**
- 自动构建 `app.asar` 并推送到 Cloudflare R2 生产节点（`https://r2.053999.xyz/Outmap/version.json`，v1.9.40）。
