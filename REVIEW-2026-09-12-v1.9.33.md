# Outmap v1.9.33 审查与发布说明

## 结论

Antigravity 提出的十项优化方向总体成立。本版按现有架构克制落地，未引入新进程、后台轮询或第三方依赖，也未修改离线地图目录及收藏数据格式。

## 本版处理

- 路线点与收藏点保持 MapLibre 原生 GeoJSON 聚合。修正聚合展开被固定限制在 13 级的问题，展开层级改为地图原生结果并受地图最大级别约束。
- 路线途径点由蓝色改为靛紫色，聚合数量改为琥珀/橙色色阶；起点绿色、终点红色保持不变。混合缩放层级下，单点与“数量聚合”不再被误认为重复点。
- 提高聚合有效层级并通过多缩放级别运行测试核对 `getClusterLeaves`，确保任何聚合成员不会同时出现在单点层；最高视级恢复为单点。
- 六处阻塞式原生确认框统一为异步 Fluent 毛玻璃确认框，支持遮罩、Esc、回车、危险操作色和焦点恢复。
- 全局搜索增加上下键高亮、滚动和回车选择；省市仅使用内置 `pinyin`、`py`、英文数据本地匹配，不生成或猜测地名数据。
- 手机抽屉达到关闭阈值后直接沿手势方向退出，不再先回弹；当前分类增加独立管理入口且不抢占长按排序。
- 桌面精确指针环境支持窗口级轨迹文件拖入，多个导入入口复用同一解析流程；限制单文件与 64 MB 上限，避免误导航和大文件阻塞。
- 网页剪贴板在 HTTP/受限权限环境增加兼容兜底；同步存储捕获配额与隐私模式异常；触控结束、取消及窗口失焦均复位地图拖动状态。
- 删除重复 `escapeHtml` 实现。

## 稳定性边界

- 聚合和分散单点在同一缩放级别共存是空间聚合的正常行为：相近点聚合，较远点保持单点。本版通过不同颜色明确语义，并以成员互斥测试排除“同一个点被渲染两次”。
- 未关闭全局图层淡入动画，没有用牺牲视觉效果的方式规避问题。
- 未对性能提升比例作无实测依据的承诺。

## 发布门禁

- 新增 `scripts/test-v1.9.33.cjs`：覆盖拼音/简拼不联网、Fluent 确认、聚合成员互斥、多缩放级别和最高视级展开。
- `npm run test:release` 继续包含历史相机、路线规划、移动端、收藏排序、下载与运行时回归测试。
