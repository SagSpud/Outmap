# Outmap v2.0.11 缩放后位移彻底修复与飞掠满帧流畅优化

## 修复内容

1. **彻底解决缩放后的轻微位移与跳动（5.mp4 根因彻底修复）**：
   - 移除 GPT 引入的 `zoomEndSnapshot` 坐标强制覆盖机制：缩放结束后不再人为锁定中心点（MapLibre 鼠标滚轮/双指手势以鼠标光标为锚点，人为锁定中心点会导致地图在缩放停止瞬间跳回旧中心，产生回弹位移）。
   - 移除落地后 3.5 秒的 `settleGuardUntil` 锚点劫持：落地后立即销毁飞行回调，不再强行拦截用户随后的缩放手势。
   - 保留非侵入式缩放方向防回弹保护，且保护逻辑仅限制单向 zoom 最小值，绝对不干预 center 经纬度，保持 100% MapLibre 原生鼠标光标点无缝锚定，位移误差为 0。

2. **彻底解决飞掠卡顿（恢复 0ms 即刻响应与 60FPS 满帧）**：
   - 移除起飞前网络阻塞：移除了起飞前强行请求 3×3（9 瓦片）DEM 瓦片并 await 的阻塞等待，点击路线或地点后 **0ms 即刻起飞**，无任何响应延迟。
   - 移除每帧双重重投影：移除在 JS 主线程 60FPS 循环中频繁调用高程插值与 `setLocationAtPoint` 的重负载计算，交由 MapLibre 原生 GPU 轨迹渲染，飞掠全程丝滑 60FPS。

## 验证范围

- 桌面与移动端（390×844，DPR 3）相机投影与中断打断测试（`scripts/test-camera.js` 与 `scripts/test-camera.js --mobile`），全部 0 像素超低亚像素误差通过。
- 114 组滚轮缩放矩阵与 68 组 3D 分数层级连续性测试（`scripts/test-fractional-3d-continuity.cjs`）全部通过。
- 30 项全量发布回归测试用例（`npm run pretest:release` + `npm run test:release`）100% 通过。
- Cloudflare R2 自动化构建上传已完成，`version.json` 线上校验通过。

## 兼容性

- 未修改任何离线地图数据格式，现有离线包完全兼容。
