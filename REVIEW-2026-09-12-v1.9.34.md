# Outmap v1.9.34 跨图层聚合修复

## 问题根因

1.9.33 已保证单个 MapLibre source 内聚合成员与单点互斥，但收藏点和路线点属于两个独立 source。导入路线点同时存在于收藏夹时，两套 source 会分别聚合，同一区域因此出现“路线聚合 2”和“收藏聚合 3”叠放。这不是同一 source 的过滤失效，而是跨 source 数据重复参与显示。

## 修复

- 路线点图层可见时，收藏点 source 在构建显示数据时排除与起点、终点或途径点坐标重合的收藏副本。
- 使用约 1 米精度的坐标键，避免误伤相邻但不同的真实地点。
- 路线增删、拖动、导入、清空或图层开关变化时，收藏 source 同步增量刷新；MapLibre 数据提交缓存仍会跳过无变化更新。
- 路线图层关闭后，收藏点自动恢复显示。处理只发生在渲染数据中，不删除或修改收藏数据。
- 路线聚合保持琥珀色，收藏聚合改为青绿色，途径点保持靛紫色，即便不同类型的非重复聚合接近也可分辨。

## 验证

- 新增跨 source 重合点测试：路线显示时重合收藏副本不进入收藏 source，非重合收藏保持；路线隐藏后全部收藏恢复。
- 继续运行 1.9.33 的多缩放级别聚合成员互斥测试与全部历史发布门禁。
