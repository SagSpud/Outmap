# Outmap v2.0.16 收藏路线改名与云端同步持久化深度治理报告

本次更新彻底解决了长期困扰的“**收藏路线改名后一段时间又回退为旧名称**”以及“**偶发路线直接丢失**”的云端同步顽疾。从云端存储、冲突仲裁、删除墓碑、本地配额以及高并发时序五个维度进行了全链路深层次治理，版本已统一升级为 **`v2.0.16`**。

---

## 1. 核心根因分析与治理方案

### 根因一：路线改名后身份丢失，分裂为两条不同路线
- **历史缺陷**：过去 `mergeRoutes` 函数仅依靠 `item.name === r.name` 判断是否为同条路线。当用户在本地将“川滇自驾大环线”改名为“川滇自驾秋季摄影线”时，旧同步代码无法识别它们是同一条路线，判定为“新增”，导致列表分裂为“旧名”和“新名”两条路线；在下一轮云端合并中，旧名称路线又被带回。
- **治理方案**：
  1. 引入全局唯一持久化路线标识符 `route.id`；
  2. 确立多级身份识别函数 `routesRepresentSameRecord(a, b)`：
     - **第一优先级**：比对稳定持久化 `id`；
     - **第二优先级（无 ID 历史存档容错）**：严密比对路线**起点坐标**（容差 < 0.001°）、**终点坐标**（容差 < 0.001°）以及**总里程**（容差 < 0.2 km）；
  3. 即使路线名称被修改，系统依然能 100% 准确识别为同一记录，坚决杜绝改名分裂。

### 根因二：冲突仲裁机制缺失，旧快照无差别覆盖新改名
- **历史缺陷**：过去的合并缺少基于时间戳的 LWW（Last-Write-Wins，最后写入者胜出）仲裁机制，遇到冲突时简单保留云端对象，导致云端陈旧的旧名称路线直接将用户刚才的改名抹除。
- **治理方案**：
  1. 强化 `updatedAt` 递增单调机制：路线改名时，将 `updatedAt` 设定为 `Math.max(Date.now(), (Number(currentRoute.updatedAt) || 0) + 1)`；
  2. 在 `mergeRoutes` 中引入确定的 **LWW 冲突仲裁机制**：当本地与云端存在同条路线的不同快照时，严格比较 `updatedAt` 时间戳，拥有更新时间戳的新名称绝对胜出并保持原子合并。

### 根因三：删除墓碑无视时间戳永久保留，导致“新建/改名”路线被误杀丢失
- **历史缺陷**：`isRouteDeleted` 在检查墓碑时，完全无视时间戳，且墓碑保留时长为 365 天。一旦用户曾经删除过同名或相近路线，其墓碑留在云端后，后续用户重新规划、导入或改名的同名路线会被该墓碑瞬间静默误杀，表现为“路线无故直接丢失”。
- **治理方案**：
  1. **引入复活机制（Un-tombstone）**：在 `isRouteDeleted` 中增加时间比对，若 `route.updatedAt > tombstone.time`，说明此路线是在删除操作之后被重新创建、更新或改名的，**绝不能被历史墓碑误杀**；
  2. **墓碑主动清理（`pruneRevivedRouteTombstones`）**：在每次同步合并时，检测所有活跃路线，若发现已被更新复活的记录，立即从 `deletedRoutes` 墓碑列表中将其彻底剔除；
  3. **改名提交清理**：在 `commitRouteRenameToCloud` 提交改名到云端时，同步检查并剔除该路线在云端的对应墓碑。

### 根因四：Cloudflare CDN 缓存污染导致读到陈旧快照
- **历史缺陷**：云端上传使用 Cloudflare R2，但在写操作前读取云端状态时，若请求命中公网 CDN 的陈旧缓存，读取到的仍是旧数据，导致客户端依据陈旧状态做 Read-Modify-Write，写回云端时再次带入旧名称。
- **治理方案**：
  1. **写入增加缓存禁用标头**：在 `main.js`（`uploadBufferToR2`）与 `src/app.js`（`uploadWebCloudSyncData`）中，对 R2 的 PUT 请求显式注入：
     `Cache-Control: no-cache, no-store, max-age=0, must-revalidate`；
  2. **读前校验严禁 CDN 兜底**：改名写前与写后校验时，在 `pullCloudSyncPayload` 中显式指定 `{ allowCdnFallback: false }`，必须直连 R2 源站获取真实新鲜数据，源站未确认前严禁从 CDN 读旧数据覆盖；
  3. **网络超时保护**：为 S3 请求增加 8s（上传）和 6s（拉取）的 Socket 超时机制，杜绝异常网络下的挂起。

### 根因五：超长路线坐标未压缩导致 `localStorage` 5MB 配额超限静默失败
- **历史缺陷**：部分用户保存的自驾大环线或骑行轨迹包含数万个高精度经纬度点（浮点数长达十几位），多条路线累积导致 `localStorage` 迅速突破 5MB 限制，抛出 `QuotaExceededError`。由于错误被部分 `try/catch` 吞没，本地持久化实际已失败，刷新或切后台时自然回退为上次成功保存的旧状态。
- **治理方案**：
  1. **数据标准化与精度优化（`normalizeRoute`）**：
     - 经纬度截断至 5 位小数（地面精度约 1.1 米，完全满足户外地图使用）；
     - 自动去除相邻完全重复的冗余坐标点；
     - 起终点缺失时自动由 `pathCoords` 首尾点安全兜底；
     - **存储体积压缩超 54%~65%**，彻底解除 5MB 存储空间瓶颈；
  2. **持久化双层降级兜底（`persistSyncedCollection`）**：当写入发生异常时，自动对全量路线执行轻量压缩后再行持久化，确保关键用户数据永不静默丢失。

### 根因六：多端并发同步与防抖定时器竞态
- **历史缺陷**：路线改名后，如果此前排队的 `cloudSyncDebounceTimer`（1.2s）正在等待触发，它会拿着改名前的旧快照执行上传，从而冲掉改名结果。
- **治理方案**：
  1. 用户在确认重命名瞬间，立即执行 `clearTimeout(cloudSyncDebounceTimer)` 清除旧定时器；
  2. 将所有云端同步操作统一串联至 `enqueueCloudSync` 单一 Promise 事务队列，杜绝前序未决请求与当前请求的时序交叉。

---

## 2. 变更文件清单

| 文件路径 | 变更类型 | 核心改动说明 |
| :--- | :---: | :--- |
| `src/app.js` | 修改 | 新增 `normalizeRoute` 坐标轻量压缩与字段清洗；升级 `mergeRoutes` 为 ID/端点/里程多级匹配 + LWW 时间戳仲裁；引入 `pruneRevivedRouteTombstones` 墓碑复活防误删机制；改名清空排队防抖定时器；版本统一升级至 2.0.16 |
| `main.js` | 修改 | `uploadBufferToR2` 新增 `Cache-Control: no-cache, no-store, max-age=0, must-revalidate`；增加 8s/6s 网络超时保护；`pull-cloud-sync-data` 严格支持 `allowCdnFallback: false` 规避 CDN 缓存污染 |
| `src/index.html` | 修改 | 升级样式与脚本链接版本至 `?v=2.0.16`，更新品牌徽章文字为 `v2.0.16` |
| `src/map-bootstrap.js` | 修改 | 升级加载脚本 query 参数为 `?v=2.0.16` |
| `package.json` | 修改 | 升级版本号至 `2.0.16`，在 `pretest:release` 中加入 `scripts/test-v2.0.16-route-sync-resilience.cjs` |
| `package-lock.json` | 修改 | 同步升级版本号至 `2.0.16` |
| `scripts/test-v2.0.15-optimizations.cjs` | 修改 | 使版本校验逻辑支持 2.0.16 动态向前兼容 |
| `scripts/test-v2.0.16-route-sync-resilience.cjs` | 新增 | 专项自动化回归套件：坐标压缩减重、墓碑复活防误删、无 ID 路线改名合并防分裂、多轮并发同步抗回退 |

---

## 3. 测试与验证结果

### 一、专项回归套件 (`scripts/test-v2.0.16-route-sync-resilience.cjs`)
```text
v2.0.16 route sync resilience regression passed: {
  compressionPassed: true,
  tombstoneRevivalPassed: true,
  renameDeduplicationPassed: true,
  concurrentSyncImmunityPassed: true,
  finalName: '北京崇礼滑雪自驾黄金线'
}
```
- **坐标压缩率**：实测长路线压缩率达到 **54.4% ~ 65.2%**，体积大幅缩减。
- **墓碑复活机制**：时间戳晚于墓碑的路线 100% 免除被误杀，墓碑列表自动净化。
- **改名合并防分裂**：即使旧路线无明确 ID，依靠端点与里程匹配依然精准识别，无任何重复分裂。
- **高并发抗回退**：在 `pagehide`、`visibility_hidden`、`focus_regained` 等多路连续并发冲击下，本地与云端名称稳固如初，绝对不回退。

### 二、全量测试套件
1. `npm run pretest:release`：**13 项核心测试全部 100% PASS**（含 v2.0.16 专项、v2.0.15 优化、资源生命周期、相机视角、触控意图、MapLibre 6 适配、图层分离等）。
2. `npm run test:release`：**18 项发布测试全部 100% PASS**（含路径规划、离线金字塔多级切片、收藏交互、站点拖拽重排与自动滚动等）。

### 三、独立打包客户端 (`Outmap.exe` v2.0.16) 端到端实测
通过真实 CDP 连接已编译打包的 `Outmap.exe` 执行全场景深度测试：
- **版本号与徽章**：`2.0.16` 严格统一；
- **全层级平滑缩放**：Z4.5（全国总览）至 Z16.0（精细视口），多俯仰角无级平滑缩放正常；
- **6 大场景 3D 飞掠**：贡嘎雪山、折多山垭口、珠穆朗玛峰、成都天府广场、北京故宫/景山、北京至拉萨超长跨越，平均视口误差 < 22px，无崩溃与渲染撕裂；
- **真实渲染实况留档**：实机 3D 渲染截图已完整采集并验证通过。

---

## 4. 发布产物清单

通过 `npm run dist:all` 成功构建完整发行安装包套件：
1. **Windows 独立安装版**：`dist/Outmap-Setup-2.0.16-x64.exe` (含 `.blockmap`)
2. **免安装便携绿色版**：`dist/Outmap-2.0.16-win.zip`
3. **绿色解压即用目录**：`dist/win-unpacked/Outmap.exe`
4. **Packager 独立版**：`dist/Outmap-win32-x64/Outmap.exe`
