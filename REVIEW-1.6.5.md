# Outmap 1.6.5 稳定性审查与变更记录

## 审查范围

- 基线：1.6.4（提交 `6b12eb5`）。
- 回看 1.5.2—1.6.4 的提交和 Antigravity 近期会话/brain 记录。
- 重点：三模式路线、超长多点、请求取消与缓存、路线拖动、面板动画、移动端 GPU 与省电。

## 已确认并修复的问题

1. 骑行/徒步主服务失败后曾回退到公共驾车 OSRM，并把结果写入 bike/foot 缓存。1.6.5 仅允许驾车使用该备份；持久缓存增加 schema、profile、provider 校验，旧 bike/foot 缓存自动失效并自愈。
2. 桌面端曾由本地代理重试两次后，渲染进程再重复请求两次，弱网单批可能等待约 24 秒。1.6.5 由本地代理单独负责缓存和故障转移，渲染端直接接受明确标注的离线导引结果。
3. 多点逐腿降级只要一腿成功就会把整批标成真实道路。现改为所有腿都成功才标记路网匹配；部分失败保留导引标识。
4. 大地线降级原来统一按 48 km/h，导致骑行、徒步时间错误。现分别按自驾、骑行、徒步速度估算。
5. 连续设置 62 个点会产生 119 次取消请求。状态变更现以 100 ms 尾随合并，测试降为 0 次无效取消；真正覆盖进行中请求时仍会取消旧任务，并发保持最多 2。
6. 路线逐腿内存缓存无上限。现增加按条目数和总顶点数双限制的 LRU，降低长时间使用后的内存增长风险。
7. 路线规划中不再重复执行完整 DEM 剖面采样，先用轻量虚线导引反馈，真实路网完成后一次性生成统计与剖面。
8. 路线拖动曾混用可见 DOM 索引和有效数据索引，空起终点时会错站；固定行高也无法跨嵌套容器准确移动。现按有效行和真实矩形中心计算，限制拖出范围，pointer cancel/窗口失焦只回滚不提交。
9. 松手过渡曾被 `.is-dragging { transition:none !important }` 抵消而闪跳。现拆分 dragging/settling 状态，并用会话令牌取消陈旧提交。
10. 途经点名称曾直接插入 HTML value，特殊字符可能破坏列表 DOM。现通过 DOM value 属性赋值。
11. 面板/弹窗关闭使用不可取消定时器，快速关闭后重开会被旧回调再次隐藏。现统一为 animationend + 超时兜底的可取消状态机，支持系统“减少动态效果”。
12. 去除退出动画中的实时 blur，仅保留 transform + opacity；永久 will-change 改为仅动画/拖动期间启用。手机地图移动时临时降低毛玻璃采样半径，停止后恢复完整效果。
13. FPS 显示曾永久运行 requestAnimationFrame。现统计真实地图 render 事件并每秒更新，地图静止与后台时不再逐帧唤醒。
14. 手机高程控件的 click + touchend 双绑定可能一次触摸切换两次，现统一使用 click/pointer 合成路径。
15. Tooltip 抑制曾在每次 mouseover 查询 DOM，现改为低频 MutationObserver。

## 回归结果

- 50°、70°、2D、快速替换和移动端抽屉下的地点飞掠投影测试通过。
- 起点、终点、途经点联想框均保持在 visual viewport 内。
- 自驾、骑行、徒步独立 profile 测试通过。
- 62 节点拆为 9 个连续批次，节点顺序完整，并发上限 2。
- 超长移动端路线几何限制、路线位于道路之上且路名之下测试通过。
- 快速关闭/重开面板和替换提示框竞态测试通过。
- 桌面弱网导引只请求本地代理一次，并明确显示“导引”。
- 完整 `npm test` 全部通过。

## 数据与发布兼容性

- `offline-tiles` 继续位于程序外部目录，未纳入安装包或 ASAR，也未改动瓦片结构。
- 已在线成功缓存的驾车路线继续兼容；旧版可能被驾车备份污染的骑行/徒步缓存会自动重建。
- 仍采用“在线成功后缓存、相同路线可离线复用”的方案，不引入全国首次断网路由图包。
