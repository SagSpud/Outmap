# Outmap v2.0.8 飞掠稳定性复核

## 录屏结论

- 录屏最后一次飞掠在高山目的地 DEM 到达前后出现纯底色画面，缩放或拖动后恢复。
- 原因是旧的最终相机回调在 MapLibre 原生地形防穿透之后重新写回 zoom、pitch 和 elevation，可能把相机压入刚加载的地形。
- 旧实现还会在 `sourcedata`、`idle` 后最多重复四次短校正，形成落地后被拉回的观感。

## 修复

- 飞掠主过程和地形防穿透完全交给 MapLibre 6.9 原生相机。
- 目的地 DEM 晚到时，仅在一个有界收尾阶段平滑同步目标地表高程，不覆盖原生计算出的安全 zoom、pitch 或 center。
- 如防穿透改变了最终视角，只执行一次使用安全视角的重新锚定；随后立即释放监听，不会被后续瓦片事件反复拉回。
- 搜索、收藏点、收藏夹列表、路线起点、终点、途经点、省份均共用 `flyToLocationPrecisely`；路线搜索候选一次选择只启动一次飞掠。
- 无 DEM 或离线缺瓦片时设有 8 秒上限；任何滚轮、拖动、触控、键盘输入或新的飞掠都会立即取消旧状态。

## 验证范围

- 本地确定性 6000 米 DEM：2D、50°、70°，L4.45、L12、L13、L14.8、L15。
- 真实在线高山 DEM：拉萨及高山近景，包含冷数据落地与原生防穿透调整。
- 远近距离、快速连续飞掠、外部相机替换、用户中断、覆盖层滚轮、触控缩放、键盘与双击缩放。
- 路线三模式、62 个途经点、收藏点桌面/移动交互回归。

## 数据兼容

- 未修改 DEM、OSM、等高线、收藏或路线的存储格式和目录；现有离线地图无需重新下载。
