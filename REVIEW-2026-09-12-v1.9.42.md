# Outmap 1.9.42 优化与修复说明

## 用户反馈与根因分析

### 反馈内容
> “现在做了优化之后 拖动缩放 图标图层先不显示 然后才出来 体验不好 这个是不是前两个版本做的优化 要不去掉吧”

### 根因分析
1. **优化引入溯源**：
   - 确实是在数个版本前的 `v1.9.35`（`Optimize desktop map motion scheduling`）中引入了 `src/map-performance.js`。
   - 当时的优化出发点是尝试在高分屏设备（Windows 125% / 150% / 200% 缩放）上，在拖动、缩放地图时动态降低分辨率（`adaptivePixelRatio`）并在运动期间暂退密集 POI 图标，以期降低 3D 渲染开销。
2. **为何会导致“拖动缩放 图标图层先不显示 然后才出来”**：
   - MapLibre GL 属于 WebGL 矢量渲染引擎，文字和图标的排布依赖全局碰撞检测树（`symbol-collision`）。
   - 在用户拖动或滚轮缩放地图的瞬间（持续运动约 150ms），调度器动态调用了 `map.setPixelRatio(target)`。这强制底层 WebGL 画布（Framebuffer）触发重置与重设（`resize`），**直接清空了所有已排布的图标和文字几何网格，造成图层瞬间消失**；
   - 当用户松开鼠标结束拖动后（`moveend`），调度器又在 180ms 后再次调用 `setPixelRatio(undefined)` 恢复原生 DPR，**导致画布二次重置，图标与文字再次重新加载计算，用户便看到了“先不显示、松手后才延迟跳出来”的严重迟滞感与闪烁**；
   - 此外，调度器内部的 `SECONDARY_FLIGHT_LAYERS` 还在飞行时硬编码将 `osm-all-pois`、`osm-all-pois-dots` 等图层设为 `visibility: 'none'`，进一步加剧了图标图层缺失感。

---

## 修复措施

遵照用户要求，**彻底彻底拔除该动态调度降分辨率与图层暂退逻辑**：
1. **完全废除动态分辨率下调**：
   - 移除 `sampleRenderFrame` 中的 `setPixelRatio` 调用，全场景保持显示器原生 DPI 渲染，杜绝任何运动中的画布 resize；
2. **完全废除图层隐藏与透明度渐隐**：
   - 移除 `SECONDARY_FLIGHT_LAYERS` 隐藏机制，`osm-all-pois`、`osm-all-pois-dots`、收藏点、路线途径点等全量图标在拖拽、缩放、飞跃中 100% 全时常驻渲染；
3. **安全退役与轻量化封装**：
   - `src/map-performance.js` 重构为零开销的安全空对象，不再向地图监听 `movestart`、`moveend`、`render` 事件，彻底释放主线程事件开销；
   - 清理 `src/app.js` 内的 `__outmapInternalQualityResize` 阻断逻辑，恢复最纯粹、最平滑的原生 GPU 渲染体验；
4. **自动化回归门禁**：
   - 更新 `scripts/test-desktop-map-performance.cjs`：验证在 100%、125%、150%、175%、200% DPI 下运动时 DPR 100% 恒定不变，图标图层全时可见；
   - 新增 `scripts/test-v1.9.42.cjs`：在离屏无头环境下实测地图拖拽与缩放过程，确保所有图标图层始终可见，杜绝图层被置为 `visibility: none` 或降透明度。

---

## 验证与发布

- 运行 `node scripts/test-desktop-map-performance.cjs`：**PASS (100% 原生 DPR 与图标常驻验证通过)**
- 运行 `npx electron scripts/test-v1.9.42.cjs`：**PASS (100%)**
- 运行 `npm run pretest:release`：**PASS (100%)**
- 运行 `npm run test:release`：**PASS (全量回归套件全部通过)**
- 自动打包构建 `app.asar` 并推送到 Cloudflare R2 生产节点（`v1.9.42`）。
