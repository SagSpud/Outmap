# 1.9.31：审查 1.9.24 之后的变更

范围：`f443350`（1.9.24）至 `20005eb`（1.9.30），以及 Antigravity 原会话目录中的最新 walkthrough。以源码和实际运行结果为准，不把 walkthrough 中的“100%”作为测试证据。

## 确认并修复

1. **阻断启动**：1.9.30 的 ESC 调度中多出一个右括号，`node --check src/app.js` 直接失败。修复后完整脚本可解析，增加全脚本编译检查、运行回归与打包前语法门禁。
2. **地图重复动作**：收藏路线内线与描边分别绑定点击，聚合圆与数字分别绑定点击，一次命中可能加载/飞掠两次。改为各自完整命中区域的单次监听；地图选点时不加载收藏路线。移除动态 hover 圆自身的 hover 监听，避免其出现/消失干扰高亮。
3. **收藏坐标过滤回归**：纬度用真值判断漏掉纬度 0，且接受 Infinity。恢复有限值与经纬度范围验证；本地搜索也过滤异常坐标。不改变导入文件的坐标系，不擅自转换用户数据。
4. **搜索性能与并发**：精确本地命中不再因不足三个候选而等待联网；不同路线输入框的请求不互相取消，同一输入框的新请求取消旧请求。响应体读取仍在超时保护内；异常 GeoJSON 单条记录不会中断后续有效候选。收藏描述显示分类名称而非内部 ID。
5. **手机手势**：取消/多指触摸不再关闭抽屉；下滑与回弹使用独立 translate 和浏览器 Web Animations，保留原 transform、减少动态效果偏好，不重复绑定。控件点击不启动拖动。
6. **长按冲突**：收藏点长按不再叠加普通地图菜单；长按后的合成点击不触发收藏飞掠。离开图标、移动地图或 touchcancel 均清理长按计时器；收藏列表补充取消清理。
7. **拖动开销**：收藏分类拖动预览用 transform 更新而非每帧 left；滚动到边界停止无意义 rAF，下一次指针移动再唤醒。保留相邻分类让位动画、取消恢复和现有排序逻辑。
8. **发布可靠性**：上传失败不再被打包脚本吞掉；发布说明更新为当前版本。历史测试的版本白名单改为实际版本一致性，功能断言不删除。

## 保留的有效方案

- 1.9.26–30 的统一 SVG / Path2D 图标、系统原生鼠标指针、紧凑收藏列表和 viewport 对齐。
- 1.9.29 的安全相机锚点、现有 MapLibre 原生 flyTo/easeTo 和地形落点计算。
- 收藏点、路线点、聚合与收藏路线仍是 MapLibre 原生图层；DOM 抽屉/分类排序使用浏览器原生动画 API，不强行塞进地图。
- 动画和透明玻璃保留；无新增常驻进程、轮询或空闲动画循环。
- 下载完成层级直接跳过，未完成层级按目录找缺片，压缩任务队列、地图运动时下载让出资源保持不变。本轮不改 main.js 下载逻辑，也不扫描/清理用户离线目录。

## 验证与边界

发布测试入口：`npm run test:release`。

- 新增运行测试：本地搜索零联网、跨输入并发、同输入取消、畸形候选、慢响应体超时；手机取消/多指/回弹/重复绑定。
- 实际页面菜单测试：960×540 CSS 视口 / DPR 2（200% 高 DPI）与 390×400 / DPR 3；删除项可命中、未被遮挡，菜单宽度分别 88、94 CSS 像素。
- 相机测试：桌面与手机，0°/50°/70°、远距离、连续替换、中断、晚到 DEM、底部抽屉；无额外纠偏 jump。
- 路线测试：驾车/骑行/徒步、62 个路线点的分段规划、最多 2 个并发请求、旧请求取消、路线在道路之上而文字之下、手机起终点/途径点联想框在视口内。使用受控路线响应，不代表所有实际道路与在线服务永远可用。
- 原生图层与空闲测试：52 个路线点；首次测试静止 1.6 秒 render 事件为 2 次，三个面板均保留 blur(20px) saturate(1.6)。这是渲染次数，不是 GPU 占用率，也不是相对于 1.9.24 的性能提升百分比。
- 离线测试：库存计算、L10 完整/L11 部分状态、省份选择不被刷新改变、缺片压缩枚举、DEM 层级边界。未重下全国地图，未验证源站每个缺片。
- 导入测试：GPX、KML、GeoJSON、Outmap JSON 的 22,104 点路线与起终点/11 途径点。

仍需外部证据的问题：白鹭金岸原始导入坐标偏差需要原文件；源站 404 不能直接视为“真实空瓦片”。当前未增加持久化 404 缓存，历史零字节 PBF 也无法仅凭大小区分有效空数据与损坏文件。本轮不宣称这些历史数据问题已解决。真机 Safari、持续满速下载时的 GPU 百分比与端到端帧时间未在本轮实测。
