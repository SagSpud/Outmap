# 1.9.32 性能与交互优化

在 1.9.31 上完成六项优化，保留动画、玻璃效果、图标及现有数据格式。未删除、重建或扫描用户离线地图；未新增常驻进程。

## 实现

1. **下载查缺片调度**：最多积压 256 个压缩缺片范围（不是 256 块瓦片）。消费者释放范围后唤醒生产者，慢网络不再让任务范围无限积压。地图运动时目录发现每轮主动让出 24ms；静止时不加此等待，保持 36 路下载和现有交互时 4 路策略。取消会唤醒等待，工作任务异常时终止并等待剩余任务退出后才释放下载所有权。
2. **路线聚合点击**：只在聚合圆命中区域绑定点击，数字层不再重复启动缩放。
3. **海拔图**：按数据数组、点数、画布尺寸和 DPR 缓存曲线/网格背景；鼠标与触摸按帧合并，采样点没变化不重复绘制或移动联动标记。尺寸变化重建缓存，离开和取消触摸取消待执行帧。仍保留原曲线样式、竖线和地图联动。
4. **途径点列表**：按稳定 ID 复用节点，不再每次清空重建。新增、删除、重新排序刷新动态索引；保留未提交的输入草稿、焦点和选择范围。移除行取消搜索，防止异步结果写入已删除点。拖动仍使用现有动画引擎。
5. **路线排序空转**：只有确实滚动时才连续调度自动滚动帧。位于中间或到达边界后停帧，下一次指针移动再唤醒。
6. **同步与地图更新**：同步合并后的收藏只序列化一次用于比较/写回，内容一致不写 localStorage、不重载列表。MapLibre 数据源按 feature ID 比较，忽略完全相同的更新，只通过原生 updateData 提交增删改；不支持该方法的源使用 setData。源对象更换后重新初始化缓存。保持现有先拉取、合并、串行上传和删除墓碑协议，不迁移存储、不减少数据安全步骤。

另外修复原收藏点 updateData 的属性格式：此版本 MapLibre 需要 `{key,value}` 数组，原代码传对象会忽略图标/名称变化。本轮已用工作线程实际数据验证名称、图标、坐标和删除。

## 测试与可量化结果

新增 `scripts/test-v1.9.32.cjs`，纳入 `npm run test:release`。

- 队列容量、消费者释放后唤醒、取消等待及交互等待取消。
- 60 个途径点新增后原节点复用 **60/60**，草稿与焦点保留；重排后删除、搜索编辑命中正确 ID。
- 地图测试：首次 1 次 setData、变更 1 次 updateData，**20 次完全相同提交全部跳过**。通过公开 getData 读取工作线程结果，检查增删改和属性。不能用 `_data` 检查 updateData：该内部字段只保留最后一次 setData 输入。
- **10,000 点曲线、100 次悬停**：初次生成背景遍历 3 遍，随后悬停没有再遍历全曲线；尺寸改变能重新构建缓存。
- 发布回归继续覆盖桌面/手机 0°、50°、70°飞掠；62 点路线规划、三种出行方式；站点角色重排和连续地图选点；200% 高 DPI / 手机菜单；52 点原生图层、空闲渲染与玻璃一致性；离线库存状态和缺片范围；22,104 点路线导入。

这些数字是受控测试中的重复工作削减，不是整机 FPS/GPU 提升百分比。未进行全国 L14 满速真实下载前后对照，也未测真机 Safari。下载仍受源站、网络与磁盘限制；404 和历史异常文件问题沿用 1.9.31 审查说明，未把无数据伪装成下载完成。

同步仍使用兼容的 localStorage 和现有全量上传协议，因此大数据首次序列化仍有成本；本轮去除重复写入与重绘，没有实施风险更高的数据库迁移或跳过云端合并。
