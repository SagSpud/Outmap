# Outmap 1.9.37 修复说明

## 问题原因

收藏数据与图层系统会在 MapLibre `load` 事件之前初始化。1.9.36 拆分图层后，收藏路线的首次渲染在地图未就绪时直接返回，却没有登记加载完成后的重试，因此部分正常启动时序下收藏路线 source 和 line 图层不会被创建，之后打开开关也只能修改不存在图层的可见性。

## 修复

- 收藏路线首次渲染遇到地图未就绪时，只登记一次 `load` 回调；地图加载后自动补建原生 GeoJSON source 与 line 图层并写入已读取的收藏路线。
- 打开“收藏路线”开关时主动执行图层校验和数据刷新，覆盖初始化竞态及图层尚未创建的场景。
- 收藏路线仍默认关闭，规划路线仍默认开启，两者保持独立。
- 不修改收藏数据、规划数据和离线地图数据。

## 验证

- 新增启动时序回归：收藏路线图层必须在地图加载后自动存在。
- 验证仅通过开关即可把最新收藏路线写入 source 并显示。
- 完整发布测试通过，包括 2D/3D 相机、路线规划、多途径点、收藏聚合、离线清单、移动端和静止 GPU 门禁。
