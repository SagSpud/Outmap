# Outmap v2.0.19 发布记录

- 根因：MapLibre 默认以鼠标指针为滚轮缩放锚点，指针偏左或偏右时会同步改变地图中心。
- 修复：使用 MapLibre 6.9 公开的 `scrollZoom: { around: 'center' }` 配置，保留原生惯性、3D 地形碰撞与渲染质量。
- 验证：2D/50°/70°、L3.9–L16.8、指针左/中/右位置共 118 组滚轮用例通过；飞掠与相机回归通过。
- 离线 OSM、DEM、等高线和用户数据均不受影响。
