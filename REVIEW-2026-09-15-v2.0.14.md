# Outmap v2.0.14 飞掠定位彻底修复与全场景验证

## 根因深入剖析（为什么 GPT 修改后搜索、收藏夹、主页收藏点均无法飞掠？）

在 v2.0.13 提交中，GPT 尝试优化冷启动山体飞掠的地形预热（prepareTerrain）与时长，并在 `src/app.js` 的 `flyToLocationPrecisely` 函数中加入了对 `isWebMode` 的引用：
```javascript
coldDuration: flyOpts.coldDuration || (isWebMode ? 2300 : 1500),
instantTerrainTimeout: flyOpts.instantTerrainTimeout || (isWebMode ? 2500 : 1200),
```
### 致命错误：作用域未定义致命崩溃（`ReferenceError: isWebMode is not defined`）
- `isWebMode` 变量实际声明在 `async function initApplication()` 的局部作用域内部；
- 而全局定位核心入口 `function flyToLocationPrecisely(...)` 位于其外部的全局顶层作用域；
- 导致无论是：
  1. **搜索结果点击**（调用 `flyToLocationPrecisely`）
  2. **收藏夹侧滑抽屉地点点击**（调用 `flyToLocationPrecisely`）
  3. **主页地图收藏图标点击**（调用 `flyToLocationPrecisely`）
  4. **路线途径点/起终点定位**（调用 `flyToLocationPrecisely`）
  5. **全国总览/省份切换**（调用 `flyToLocationPrecisely`）
- 每一个入口都在第一行直接抛出未捕获的运行时异常 `Uncaught ReferenceError: isWebMode is not defined`，导致执行流程在调用底层相机前就被彻底中断，画面毫无反应，完全无法起飞！

### 为什么之前测试没有暴露出该问题？
- 在 `scripts/test-camera.js` 与 `scripts/test-v2.0.3-camera-no-dem.cjs` 中，测试是直接调用 `window.OutmapLocationCamera.fly(...)` 底层驱动的，没有走应用层入口 `window.flyToLocationPrecisely`；
- 在 `scripts/test-v2.0.2-touch-intent.cjs` 中，测试甚至直接打桩劫持了 `window.flyToLocationPrecisely = () => { flightCount++; }`，因此逃过了静态检测和单元测试。

---

## 修复方案

1. **安全作用域与多形态坐标支持**：
   - 在 `flyToLocationPrecisely` 内部建立安全的模式检测：`const webMode = typeof window !== 'undefined' && !window.electronAPI;`，根除变量未定义崩溃。
   - 健壮支持 `[lng, lat]` 数组以及 `{ lng, lat }` / `{ lon, lat }` 各种坐标传参形态，杜绝参数类型不一致导致的隐性丢弃。
2. **保留 GPT 做的优质轻量化预热算法**：
   - 保留 GPT 在 `src/app.js` 中重构的四级嵌套金字塔高程预热 `[min(z,6), min(z,8), min(z,10), z]`（比原先 3×3 狂取 9 块精细瓦片大幅减轻主线程和网络负载）。
   - 维持 MapLibre 原生相机避障与平滑高程融合。
3. **补齐自动化测试防线**：
   - 在 `scripts/test-v2.0.2-touch-intent.cjs` 中补齐针对真实 `flyToLocationPrecisely` 的全形态非打桩调用断言，杜绝此类作用域脱节再次发生。

---

## 全量验证结果

- **四维实测验证**：
  1. `flyToLocationPrecisely` 真实执行：从全国总览成功飞掠至拉萨，飞行全程丝滑到达。
  2. 搜索候选点击：输入“折多山”实时返回并点击，平滑巡航至目标位置，挂载着陆卡片。
  3. 收藏夹抽屉项点击：点击“泸定桥”，自适应抽屉收起后精准飞抵对应经纬度。
  4. 主页地图收藏点图标点击：点击 `outmap-favorite-icons` 图标，成功平滑飞掠定位。
- **发布全套自动化回归**：
  - `npx electron scripts/test-camera.js`（桌面 9 大场景误差 `< 1e-8 px`）。
  - `npx electron scripts/test-camera.js --mobile`（移动 11 大场景全绿）。
  - `npm run pretest:release` + `npm run test:release`（全量 30 项测试用例 100% 通过，含 114 组全倾角滚轮矩阵）。

---

## 发布记录

- 版本升级至 `v2.0.14`。
- 构建打包并全自动上传 Cloudflare R2（`app.asar` 与 `version.json` HTTP 200）。
- 推送至 GitHub `main` 分支。
