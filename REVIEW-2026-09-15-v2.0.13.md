# Outmap v2.0.13 3D 地形飞掠稳定性修复

## 修复内容

- 保留 MapLibre 6.9 原生单次 `flyTo/easeTo`，彻底移除落地后的二次相机移动和长期缩放守护。
- 冷地点飞掠时并行预热 L6、L8、L10 与目标层级 DEM，不阻塞起飞，也不启动额外渲染器或进程。
- 修复粗层级 DEM 首先返回后过早取消精细层级请求的问题，避免精细地形在落地后才出现而造成偏移、空白或需要拖动才能显示。
- 飞行期间仅平滑注入当前相机中心高程，最后 28% 在同一原生轨迹内将目标收敛到屏幕中间偏下；不覆盖 MapLibre 的缩放、俯仰、方向和地形防穿模结果。
- 零动画定位显式执行最终地形锚点帧，避免无动画场景落点偏移。
- 飞行结束立即释放相机回调；迟到 DEM 只允许触发重绘，禁止改变相机。滚轮、拖动、触控或新飞行会立即取消旧预热。
- 桌面端仅保留 24 块解析高程缓存，手机及受限网页端保留 12 块，在维持精度的同时控制内存和解码开销。

## 验证结果

- 真实在线 DEM 桌面与 390×844、DPR 3 手机回归通过。
- 搜索、收藏、路线起终点、途径点、快速连续飞掠、零动画和移动端抽屉避让均通过。
- 真实地形落点误差稳定在 1px 内，且没有落地后二次跳转。
- 恢复并通过 114 组滚轮矩阵，覆盖 2D、50°、70°、L3.9～L16.8 以及前景/天空缩放锚点。
- 所有滚轮场景在 `moveend` 后中心与缩放漂移均为 0；MapLibre 原生高山防穿模调整保持逐帧平滑。
- `npm run pretest:release`、`npm --ignore-scripts run test:release` 全部通过。

## 兼容性

- 不修改既有离线矢量地图、DEM、等高线目录或文件格式，无需重新下载。
