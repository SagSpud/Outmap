# Outmap 1.9.43 审查、优化与修复说明

## 审查范围

复核 1.9.37 至 1.9.42 的收藏路线恢复、ESC 交互、移动端收藏管理、跨端同步、3D 路线点贴地和地图运动性能修改，并对当前生产路径做静态与离屏运行回归。

## 发现并修复的问题

1. **3D 飞掠抵达掉帧**
   - 1.9.41 在每次抵达后立即、260ms、680ms 连续三次强制重投递路线点 GeoJSON。
   - 快速切换地点时旧延时任务不会取消，可能与新飞掠和 DEM 到达叠加。
   - 现改为 DEM `sourcedata` 与 MapLibre `idle` 合并调度；每轮最终只强刷一次，旧飞掠任务由 token 取消，并有 900ms 有界兜底。
   - 地形贴地刷新只处理路线点，不再额外序列化完整收藏点 source。

2. **路线点重复事件与点击闪烁**
   - 圆点、文字和光晕重叠层曾同时绑定点击/拖动，同一次操作可能重复触发。
   - 现在仅由原生圆点命中层接收事件。
   - 普通按下不再立即创建 DOM Marker；移动超过 3px 后才进入可视拖动，点击不会先隐藏原生点再闪回。
   - 地图拖动和列表排序统一交给全局 ESC 调度，移除重复键盘监听。

3. **收藏点与收藏路线交互**
   - 桌面收藏点左键只执行飞掠，右键打开管理菜单，避免菜单悬在飞掠前的旧屏幕坐标。
   - 触屏点击在飞掠完成后通过 MapLibre `project` 获取新坐标，再显示管理菜单。
   - 收藏路线长按后抑制浏览器生成的兼容 click，避免菜单刚出现又载入路线。
   - 路线菜单按重命名、导出、删除排列，危险操作置底。

4. **跨端同步无效占用与数据不一致**
   - 修复防抖定时器执行后句柄未清空，切后台时重复触发全量同步的问题。
   - 启动、焦点恢复和手动同步在合并内容与云端相同时不再重复上传整份收藏/路线。
   - 分类顺序和内置分类名称加入独立更新时间戳及双向 LWW 合并；旧版本无时间戳数据首次升级时以已拉取云端元数据收敛。
   - 不同 ID、不同名称但距离很近的收藏点不再被错误合并；同 ID 冲突继续按更新时间处理。

5. **1.9.42 空性能控制器残留**
   - 删除已退役的 `map-performance.js`、HTML 请求、初始化调用和发布语法检查入口。
   - 保持显示器原生 DPR，不隐藏运动中的 POI/收藏图标，不动态降低 3D 画质。
   - 删除无调用的旧强制重绘入口，静止页面不增加轮询或渲染循环。

## 保留的正确实现

- 1.9.37 收藏路线原生 MapLibre 图层恢复。
- 1.9.38 ESC 取消链路和途经点视觉方案。
- 1.9.39/1.9.40 移动端表单化输入与收藏/路线/分类 LWW 数据合并。
- 1.9.42 撤销动态 DPR 降画质和运动隐藏 POI 的决定。
- 收藏路线、规划路线继续使用独立原生 source/layer，默认只显示规划路线，避免蓝绿路线叠加。

## 验证结果

- `npm run test:release`：全部通过。
- 覆盖原生 DPR、运动中图标持续可见、50°/70° 3D 飞掠、快速飞掠取消、移动端定位、60+ 途经点规划、离线清单与下载恢复、200% 高分屏菜单、收藏/规划路线隔离、拖动排序和静止渲染。
- 新增 1.9.43 回归：相邻收藏去重、分类 UI 元数据 LWW、DEM 刷新合并、路线长按兼容 click 抑制和空性能脚本清理。

本版本不扫描、不迁移、不删除现有离线地图目录，也不改写用户收藏与路线存储结构。
